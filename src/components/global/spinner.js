/* eslint-disable react/prefer-stateless-function */
import React from 'react';
import { Spinner } from 'reactstrap';

const AppSpinner = () => (
  <div>
    <Spinner className="spinnerDesign" color="primary" />
  </div>
);
export default AppSpinner;
